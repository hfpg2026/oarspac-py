[project]
name = "oarspac"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.14"
dependencies = [
    "ospac>=1.2.5",
    "typer>=0.21.1",
]

[project.scripts]
oarspac = "oarspac.cli:app"

[tool.setuptools]
packages = ["oarspac", "oarspac.core"]

[tool.uv]
package = true
